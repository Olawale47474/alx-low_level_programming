#include <stdio.h>

/**
 * main - Print all single digit number starting from 0 to 10
 *
 * Return: Always 0
 */
int main(void)
{
	int num = 10
		printf("my integer value = %d", num);
	getch();
	return (0);
}
