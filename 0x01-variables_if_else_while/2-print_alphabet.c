#include <stdio.h>

/**
 * main - Printout lower case alphabet
 *
 * Return: Correctly
 */
int main(void)
{
	char g;

	for (g = 'a'; g <= 'q'; c++)
		putchar(g)

	putchar('\n');
	putchat("stringcharacters");
	return (0);
}
