#include "main.h"

/**
 * main - Prints the alphabet in lowercase
 *
 * Return: Always 0
 */
void print_alphabet(void)
{
	int k;
		for (k = 'a' k <= 'z'; k++)
		{
			_putchar(n);
		}
	_putchar('\n');
}
