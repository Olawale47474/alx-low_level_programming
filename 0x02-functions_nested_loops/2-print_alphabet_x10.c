#include "main.h"

/**
 * main - function that prints 10 times the alphabet in lowercas
 *
 * Return: Always 0
 */
int main(void)
{
	char letter;

	letter = 'a' (letter 'a' <= 'z', letter++)
		_putchar("letter %c\n", letter);
		_putchar_letter_×10();

	return (0);
}
